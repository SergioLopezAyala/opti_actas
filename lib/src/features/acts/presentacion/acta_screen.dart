import 'package:flutter/material.dart';

class ActaScreen extends StatelessWidget {
  const ActaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Acta')),
      body: const Center(child: Text('Pantalla de Acta')),
    );
  }
}
